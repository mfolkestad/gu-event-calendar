[project]
name = "gu-event-calendar"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Mattias Folkestad", email = "mattias.folkestad@gu.se" }
]
requires-python = ">=3.12"
dependencies = [
    "ics>=0.7.3",
]

[project.scripts]
gu-event-calendar = "gu_event_calendar:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "ipykernel>=7.3.0",
]
